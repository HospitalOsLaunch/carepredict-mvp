# Clin-JEPA Gate 1-B

Isolated research gate comparing frozen stay-record embeddings with structured
SPARCS features for three-class discharge disposition prediction.

This directory is research-only. It must not import production application code,
connect to production databases, or write outside `runs/`.
